<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Registration for new borrowers</title>
	<style>
		.sbs {
			width: 400px;
			margin: 10px;
			float: left;
		}
		.center {
			width: 70%;
			margin: 0 auto;
		}


	</style>
</head>
<body>

<div class="container">
	<div class="row clearfix">

		<div class="col-md-12 column">
			<nav class="navbar navbar-default navbar-inverse navbar-fixed-top" role="navigation">
				<div class="navbar-header">
					 <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"> <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button> <a class="navbar-brand" href="#">DojoLoans</a>
				</div>

				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
					<ul class="nav navbar-nav">
						<li class="active">
							<a href="#">New Borrower</a>
						</li>
						<li>
							<a href="/reglenders/new">New Lender</a>
						</li>
						<li>
							<a href="/users/index">Back to Main Page</a>
						</li>
					</ul>
				</div>
			</nav>
		</div>

		<div class="panel panel-primary sbs" style="margin-top: 70px">
			<div class="panel-heading">New Borrower Registration</div>
				<div id='borrower' class="panel-body">
				<%= simple_form_for @borrower, :url => regborrowers_create_path, html: { class: 'form-vertical' } do |f| %>

				  <% @tmp = @borrower %>
				  <%= render 'shared/form_regerrors' %>

				  <%= f.input :first_name, placeholder: 'First Name...' %>
				  <%= f.input :last_name, placeholder: 'Last Name...' %>
				  <%= f.input :email, placeholder: 'Email user@domain.com...' %>
				  <%= f.input :password, :input_html => { :size => 4, :maxlength => 15 }, placeholder: 'password longer than 3 chars' %>
				  <%= f.input :password_confirmation, :input_html => { :size => 4, :maxlength => 15 } %>
				  <%= f.input :needed_for, placeholder: 'Short title for need...' %>
				  <%= f.input :description, as: :text, placeholder: 'Full description of need...' %>
				  <%= f.input :amount, as: :decimal, placeholder: 'Amount looking to borrow...' %>
				  <%= f.button :submit, 'CREATE BORROWER', :class => 'btn btn-primary' %>
				<% end %>
				</div>
				<div class="panel-footer">Enter your information and click "CREATE BORROWER" to build your account.  The amount entered here is the total amount you would like to borrow on your initial loan.</div>
			</div>
		</div>

	</div>

</div>
